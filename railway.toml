[build]
builder = "nixpacks"

[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[deploy]
startCommand = "uvicorn scripts.api.server_minimal:app --host 0.0.0.0 --port $PORT"
